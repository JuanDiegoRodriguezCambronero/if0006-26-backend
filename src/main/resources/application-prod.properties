#Crear este archivo para tener la configuración al ambiente de producción (Ambiente  publicado)

# Configuración privada de la base de datos local

#spring.datasource.url=jdbc:mysql://mysql-ds3-if0006-ds3.a.aivencloud.com:16164/defaultdb
spring.datasource.url=${DB_URL}
#usar las credenciales del servicio de base de datos en Aiven


spring.datasource.username=${DB_USER}


spring.datasource.password=${DB_PASSWORD}


spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

